name: C/C++ CI

on:
  push:
    branches: [ "codespace-vigilant-succotash-779gwp64g65qhpr4w" ]
  pull_request:
    branches: [ "codespace-vigilant-succotash-779gwp64g65qhpr4w" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: configure
      run: ./configure
    - name: make
      run: make
    - name: make check
      run: make check
    - name: make distcheck
      run: make distcheck
